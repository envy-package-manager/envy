-- Loaded by lib.loads_sibling through envy.loadenv: same bundle, but reached without one.
return { bundle = ENVY_BUNDLE and ENVY_BUNDLE.identity or "<nil>",
         outer = OUTER_ASSIGNED or "<nil>" }
